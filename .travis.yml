# Enable C++ support
language: cpp

# Compiler selection
compiler:
  - gcc

# Build steps
script:
  - cd PhysiCell-1.4.1
  - make

notifications:
  email:
    on_success: always # default: change
    on_failure: always # default: always
